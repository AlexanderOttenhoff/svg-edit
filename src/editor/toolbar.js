import React from 'react';
import { Group, Button } from 're-bulma';

const Toolbar = () => (
  <Group>
    <Button>Select</Button>
    <Button>Rectangle</Button>
  </Group>
);

export default Toolbar;
